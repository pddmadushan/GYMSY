<UserControl x:Class="CMSXtream.Pages.DataEntry.StudentFilters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"   
             xmlns:sys="clr-namespace:System;assembly=mscorlib" 
             mc:Ignorable="d" >

    <UserControl.Resources >
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/ComboBoxStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/DataGridStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/IconLibrary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GroupBox x:Name="grpBoxStudent" Grid.Row="0" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName01"  GroupName="a" >Search by common search</RadioButton>
            </GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>

                <TextBox  Grid.Row="0" Grid.Column="2" Name="txtSearchText" Height="25" Margin="5,2,0,2" MaxLength="250" IsEnabled="{Binding ElementName=chkOptName01, Path=IsChecked}" />
                <ComboBox Grid.Row="0" Grid.Column="2"  Name="cmbClassGroup" Style="{DynamicResource ComboBoxFlatStyle}"  Height="25" IsEnabled="{Binding ElementName=chkOptName01, Path=IsChecked}" />
                <ComboBox Grid.Row="0" Grid.Column="1"  Name="cmbSearchType" Style="{DynamicResource ComboBoxFlatStyle}" SelectionChanged="cmbSearchType_SelectionChanged" Height="25" IsEnabled="{Binding ElementName=chkOptName01, Path=IsChecked}" />
                <mui:ModernButton Grid.Row="0" Grid.Column="3" x:Name="btnAddList" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List" Click="btnAddList_Click"  Margin="5,0,0,0" IsEnabled="{Binding ElementName=chkOptName01, Path=IsChecked}" />
            </Grid>
        </GroupBox>
        <GroupBox x:Name="grpBoxStudent2" Grid.Row="2" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName02" GroupName="a" >Search by join date</RadioButton>
            </GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>

                <mui:ModernButton Grid.Row="0" Grid.Column="3" x:Name="btnAddList2" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List"  Margin="5,0,0,0" IsEnabled="{Binding ElementName=chkOptName02, Path=IsChecked}" Click="btnAddList2_Click" />
                <DatePicker x:Name="FromDate" HorizontalAlignment="Left" Margin="92,0,0,0" VerticalAlignment="Top" Grid.ColumnSpan="2"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName02, Path=IsChecked}" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <DatePicker x:Name="ToDate" Grid.Column="2" HorizontalAlignment="Left" Margin="78,0,0,0" VerticalAlignment="Top"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName02, Path=IsChecked}" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <Label Content="Join From" HorizontalAlignment="Left" Margin="29,5,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.75,-0.062" Grid.ColumnSpan="2" Width="58"/>
                <Label Content="Join To" Grid.Column="2" HorizontalAlignment="Left" Margin="24,5,0,0" VerticalAlignment="Top" Width="54"/>
            </Grid>
        </GroupBox>
        <GroupBox x:Name="grpBoxStudent3" Grid.Row="4" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName03" GroupName="a" >Search by attendance with pending payments</RadioButton>
            </GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <mui:ModernButton Grid.Row="0" Grid.Column="3" x:Name="btnAddList3" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List"  Margin="5,0,0,0" IsEnabled="{Binding ElementName=chkOptName03, Path=IsChecked}" Click="btnAddList3_Click" />
                <DatePicker x:Name="FromDateEx" HorizontalAlignment="Left" Margin="133,0,0,0" VerticalAlignment="Top"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName03, Path=IsChecked}" Grid.Column="1" Width="116" Grid.ColumnSpan="2" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <DatePicker x:Name="ToDateEx" Grid.Column="2" HorizontalAlignment="Left" Margin="78,0,0,0" VerticalAlignment="Top"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName03, Path=IsChecked}" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <Label Content="Attendance From" HorizontalAlignment="Left" Margin="27,5,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.75,-0.062" Width="101" Grid.Column="1"/>
                <Label Content=" To" Grid.Column="2" HorizontalAlignment="Left" Margin="53,5,0,0" VerticalAlignment="Top" Width="25"/>

            </Grid>
        </GroupBox>
        <GroupBox x:Name="grpBoxStudent4" Grid.Row="6" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName04" GroupName="a" >Search by last attendance date</RadioButton>
            </GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <mui:ModernButton Grid.Row="0" Grid.Column="4" x:Name="btnAddList4" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List"  Margin="5,0,0,0" IsEnabled="{Binding ElementName=chkOptName04, Path=IsChecked}" Click="btnAddList4_Click" Grid.ColumnSpan="2" />
                <DatePicker x:Name="FromDateExAbs" HorizontalAlignment="Left" Margin="78,0,0,0" VerticalAlignment="Top"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName04, Path=IsChecked}" Grid.Column="1" Width="123" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <DatePicker x:Name="ToDateExAbs" Grid.Column="2" HorizontalAlignment="Left" Margin="46,0,0,0" VerticalAlignment="Top"   SelectedDate="{x:Static sys:DateTime.Now}" IsEnabled="{Binding ElementName=chkOptName04, Path=IsChecked}" Width="134" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
                <Label Content="From" HorizontalAlignment="Left" Margin="28,5,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.75,-0.062" Width="58" Grid.Column="1"/>
                <Label Content="To" Grid.Column="2" HorizontalAlignment="Left" Margin="10,5,0,0" VerticalAlignment="Top" Width="36"/>
            </Grid>
        </GroupBox>
        <GroupBox x:Name="grpBoxStudent5" Grid.Row="8" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName05" GroupName="a" >Search by reason</RadioButton>
            </GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="146" />
                    <ColumnDefinition Width="7"/>
                    <ColumnDefinition Width="141" />
                    <ColumnDefinition Width="106" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <ComboBox x:Name="cmbInactiveReason" Grid.Row="0" Grid.Column="1" Margin="0,4,0,0" IsEnabled="{Binding ElementName=chkOptName05, Path=IsChecked}"/>

                <RadioButton  x:Name="chkInactive" Grid.Row="0" Grid.Column="2" Margin="10,4,1,0" GroupName="chk" Content="Temporarily Inactive"  IsEnabled="{Binding ElementName=chkOptName05, Path=IsChecked}" IsChecked="True"/>
                <RadioButton  x:Name="chkInactivePernt" Grid.Row="0" Grid.Column="4" Margin="0,4,0,0"  GroupName="chk"  Content="Permanently Inactive"  IsEnabled="{Binding ElementName=chkOptName05, Path=IsChecked}" />
                <RadioButton  x:Name="chkAllInactive" Grid.Row="0" Grid.Column="5" Margin="10,4,0,0"   GroupName="chk" Content="Any Inactive"  IsEnabled="{Binding ElementName=chkOptName05, Path=IsChecked}" />
                <mui:ModernButton Grid.Row="0" Grid.Column="6" x:Name="btnAddList5" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List"  Margin="5,0,0,0" IsEnabled="{Binding ElementName=chkOptName05, Path=IsChecked}" Click="btnAddList5_Click" />

            </Grid>
        </GroupBox>
        <GroupBox x:Name="grpBoxStudent6" Grid.Row="10" Grid.Column="0" >
            <GroupBox.Header>
                <RadioButton x:Name="chkOptName06" GroupName="a" >Search by still Valid Payment but Not Attend</RadioButton>
            </GroupBox.Header>
            <mui:ModernButton x:Name="btnAddList6" IconData="{Binding Data, Source={StaticResource ImgSearch}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add to List"  Margin="100,0,0,0" IsEnabled="{Binding ElementName=chkOptName06, Path=IsChecked}" Click="btnAddList6_Click" />

        </GroupBox>
    </Grid>
</UserControl>
